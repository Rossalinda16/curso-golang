package main

import "fmt"

func main() {

	fmt.Println("Estas são as 25 palavras reservadas em Go: break, case, chan, const, continue, default, defer, else, fallthrough, for, func, go, goto, if, interface, map, package, range, return, select, struct, switch, type, var. ")
}
